{
  "name": "celo-carbon-credit-exchange",
  "version": "1.0.0",
  "description": "Decentralized Carbon Credit Trading Platform on Celo",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network celoSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network celo",
    "verify": "node scripts/verify.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.0.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
